# Sentinel-X 周总结 — 2026-W25

**周期**：2026-06-11 ~ 2026-06-17（P0 live 验收完成日 **2026-06-17**）  
**对应路线图周次**：P0 一键部署 W1–W7 全栈 + W7 inspect 触发 live  
**填写人**：Agent / 用户 live 验收

---

## 1. 本周目标（计划）

- [x] `reset-sentinel-x.sh` 安全 wipe（保留 k3s/Docker）
- [x] `install-sentinel-x.sh` 扩展 `--with-sandbox` / `--with-fixtures`
- [x] env/apply：patrol + sandbox + skills → `sync-k8s.env` / `sentinel-langgraph.env`
- [x] `verify-sentinel-x.sh --full`
- [x] DEPLOY-ONE-SHOT / DEPLOY-REFERENCE / deploy/README 更新
- [x] `sentinel-x`（47.120.6.221）全栈 install + live 验收

---

## 2. 本周完成

| 任务 ID | 内容 | 证据 / 链接 |
|---------|------|-------------|
| P0-1 | `deploy/install/reset-sentinel-x.sh` | staging 解压 → reset → 安装目录解压 |
| P0-2 | 全栈 install | `--with-ui --with-api --with-prom-sync --with-sandbox --with-fixtures` |
| P0-3 | `verify --full` | LangGraph / MCP / `list_pods=8` / sandbox 镜像 / API `/health` |
| P0-4 | `sentinel-sandbox` sync | `mcp_k8s_sync_live.py --namespace sentinel-sandbox` → `entities=21` |
| W7 live | `trigger_inspect` 手动路径 | `--pod crash-demo-857fbb5cf6-f4lrx` → `issues=['CrashLoop']` `dry_run=true` |
| 代码修复（入库） | `sync-k8s.sh` NAMESPACE 覆盖；MCP `normalize` CrashLoopBackOff；patrol 多 ns + events 兜底 | **W26 已关单** → [`2026-W26.md`](2026-W26.md) |

---

## 3. 未完成 / 推迟

| 任务 ID | 原因 | 挪至 |
|---------|------|------|
| W7 自动 patrol | 图中 Pod `status=Running`（MCP 仅 sync `phase`） | **W26 已关单** → [`2026-W26.md`](2026-W26.md) |
| `sentinel-sandbox` cron sync | 默认 cron 仅 `kube-system` | **W26 已加 cron 第二行** + `cron-sentinel-sync.example` |
| DEPLOY-ALERT-INSPECT §验收全勾选 | auto patrol 一项 pending | **W26 核心 4 项已勾选** |

---

## 4. 阻塞与风险（验收期）

| 问题 | 影响 | 处理 |
|------|------|------|
| MCP Pod status 仅 `phase` | CrashLoop 在图中显示 `Running`；auto patrol `no_candidates` | `normalize.py` 取 `containerStatuses.waiting.reason` |
| `env NAMESPACE=… sync-k8s.sh` 被 env 文件覆盖 | 手工 sync 误跑 kube-system | `sync-k8s.sh` 保留 `NAMESPACE_OVERRIDE`（已修） |
| Windows scp 无 +x | `install-deploy-scripts.sh` 报 missing | `bash` 执行 + `chmod +x`（installer 已修） |
| 双 ReplicaSet / 双 crash Pod | 演示噪音 | `kubectl scale rs … --replicas=0` |

---

## 5. 指标快照（验收完成时）

| 指标 | 数值 | 备注 |
|------|------|------|
| 服务器 | `root@sentinel-x` / `47.120.6.221` | `/opt/sentinel-x` |
| LangGraph thread_id | `5ad00ee0-6f4d-5cd6-a021-99469a86e4e1` | 与 W23 一致 |
| `verify --full` | **All checks passed** | 含 sandbox 镜像、API health |
| `list_pods` kube-system | count=8 | verify 基线 |
| `list_pods` sentinel-sandbox | count=1（sync 后） | 图中 status 暂为 Running |
| W7 inspect | `issues=['CrashLoop']` | 手动 `--pod`；`sandbox_pending=false` |

---

## 6. 下周计划（W26）

- [ ] 补传 MCP `normalize.py` + 重建 `mcp-k8s` → 图 status=`CrashLoopBackOff` → auto patrol 验收
- [ ] 补传 `patrol.py` / `sync-k8s.sh` 至服务器并 `install-deploy-scripts.sh`
- [ ] Checkpoint Phase 2 spike（W8+）
- [ ] 可选：DEPLOY-ALERT-INSPECT 验收清单全勾选

---

## 7. P0 + W7 Live 验收补充（2026-06-17）

**环境**：`root@sentinel-x`；`CLUSTER_ID=k3s-prod`；全程 **未** 设置 `SENTINEL_EXECUTE_LIVE=1`。

| Step | 操作 | 期望 / 结果 |
|------|------|-------------|
| 1 | 全栈 `install-sentinel-x.sh`（ui/api/prom/sandbox/fixtures） | venv + MCP + LangGraph + cron 就绪 |
| 2 | `verify-sentinel-x.sh --full` | **All checks passed** |
| 3 | `mcp_k8s_sync_live.py --namespace sentinel-sandbox` | `mcp pods=1 entities=21` |
| 4 | `inspect_patrol_live.py --pod crash-demo-… --dry-run true` | **`status: ok`**；`issues=['CrashLoop']` |
| 5 | `inspect_patrol_live.py --namespace sentinel-sandbox`（auto） | **W26 复验**：`CrashLoopBackOff` + auto → `issues=['CrashLoop']`；cooldown |

**验收结论**：

- **P0 一键部署 W1–W7 全栈 live ✅**（install + verify --full）
- **W7 `trigger_inspect` live ✅**（与 patrol/API 共用触发链；手动 Pod 路径已验证）
- **W7 cron auto patrol ✅**（W26 复验完成；见 [`2026-W26.md`](2026-W26.md)）

**Runbook**：[`DEPLOY-ONE-SHOT.md`](../deploy/DEPLOY-ONE-SHOT.md)、[`DEPLOY-ALERT-INSPECT.md`](../deploy/DEPLOY-ALERT-INSPECT.md)

---

## 8. 备注

- 安装时注意：**staging 解压 → reset → 再解压到 `/opt/sentinel-x`**（reset 会删安装目录）
- 手工 sync sandbox：`python …/mcp_k8s_sync_live.py --namespace sentinel-sandbox`（勿仅依赖 `NAMESPACE=… sync-k8s.sh` 直至补丁上传）
