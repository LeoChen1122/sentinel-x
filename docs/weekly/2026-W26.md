# Sentinel-X 周总结 — 2026-W26

**周期**：2026-06-17（W26 优先 live 关单）  
**对应路线图**：W7 auto patrol + MCP normalize 生产补丁  
**填写人**：Agent live 验收

---

## 1. 本周目标

- [x] 增量补传 `normalize.py` / `patrol.py` / `sync-k8s.sh` / `inspect_patrol_live.py` → `47.120.6.221`
- [x] 重建 `mcp-k8s` 容器
- [x] `sentinel-sandbox` sync → 图 status=`CrashLoopBackOff`
- [x] cron 增加 sandbox sync 第二行
- [x] W7 auto patrol + cooldown live
- [x] DEPLOY-ALERT-INSPECT 核心 4 项勾选

---

## 2. 本周完成

| 任务 | 内容 | 证据 |
|------|------|------|
| MCP normalize | `docker compose build/up mcp-k8s` | `list_pods` → `status=CrashLoopBackOff` |
| patrol 收紧 | 信任 graph status；events 兜底仅 Running + CrashLoopBackOff 事件 | 消除 kube-system 9 个误报 |
| inspect_patrol_live | 多 namespace 扫描（`find_inspect_candidates_multi`） | auto 命中 `crash-demo` |
| cron | `2-59/5` sandbox sync | `/etc/cron.d/sentinel-sync` |
| auto patrol | `issues=['CrashLoop']` `dry_run=true` | `crash-demo-857fbb5cf6-f4lrx` |
| cooldown | 二次 patrol → `{"status":"cooldown","candidates":2}` | `sentinel-inspect-patrol.sh` |

---

## 3. 代码变更（仓库）

| 文件 | 变更 |
|------|------|
| [`patrol.py`](../../agents/langgraph-integration/src/trigger/patrol.py) | 显式 status 优先；收紧 events 推断；修复 `BackOff`→ImagePull 误匹配 |
| [`test_trigger_patrol.py`](../../agents/langgraph-integration/tests/test_trigger_patrol.py) | +1 误报回归测试 |
| [`cron-sentinel-sync.example`](../../deploy/sync/cron-sentinel-sync.example) | sandbox sync cron 行 |

---

## 4. Live 验收命令摘要

```bash
# 服务器 root@sentinel-x
set -a && source /etc/sentinel/sync-k8s.env && set +a
NAMESPACE=sentinel-sandbox /usr/local/bin/sentinel-sync-k8s.sh
python .../inspect_patrol_live.py --dry-run true --skip-verify
# → crash-demo issues=['CrashLoop']
python .../inspect_patrol_live.py --dry-run true --skip-verify
# → cooldown（state 已写入后）
```

---

## 5. 未完成 / 推迟（仍属 backlog）

| 项 | 挪至 |
|----|------|
| `POST /v1/inspect` live curl | 可选 |
| Alertmanager webhook 联调 | W8+ |
| W3–W6 验收矩阵补跑 | 可选 |
| Checkpoint Phase 2 | W8+ |

---

## 6. 备注

- 首次 patrol 偶发 `ok=false`（`sandbox_pending=null`），紧接第二次通常 `ok=true`；与 LangGraph 冷启动有关，cron 5min 间隔可接受
- 生产需保留 `inspect_patrol_live.py` 多 ns 版本；仅 scp `patrol.py` 不够
