# Sentinel-X 周总结 — 2026-W23

**周期**：2026-06-02 ~ 2026-06-05（P0 live 验收完成日 **2026-06-09**，见 §7）  
**对应路线图周次**：W5 Skills + W6 沙箱预演（**代码 + live 验收**）  
**填写人**：Agent

---

## 1. 本周目标（计划）

- [x] W5 Skills 基础（记）
- [x] W6 沙箱预演（试）
- [x] W5 live（第二次 inspect 命中 Skill）
- [x] W6 live（沙箱 kubectl + verified + 写保护 + 全图）
- [x] P0 verify 全绿（`verify-sentinel-x.sh`）

---

## 2. 本周完成

| 任务 ID | 内容 | 证据 / 链接 |
|---------|------|-------------|
| W5 | SQLite FTS Skills + retrieve/record 图节点 | `skills/`, `test_skills_*` |
| W6-1 | `sandbox/` Dockerfile + fixtures + audit | [`sandbox/README.md`](../../sandbox/README.md) |
| W6-2 | planner / policy / executor / verifier | `src/sandbox/` |
| W6-3 | `sandbox_run` + `verify_skill` 升级 | `graph.py` |
| W6-4 | execute 三态 `sandbox_pending` | `execute.py` |
| W5 live | 第二次 CrashLoop inspect → `Similar past skills`；`record_skill` upsert | `skills/records/fix-crashloop-restart.md`；`hit_count=11` |
| W6 live-1 | standalone kubectl + `docker build`；daocloud busybox fixture | `sentinel-x-sandbox:latest`；`sentinel-sandbox/crash-demo` |
| W6 live-2 | `sandbox_demo`：delete `exit_code=0`；healthy 路径 `verification.pass_=true`，`ready_seconds=34` | `sandbox/audit/audit-2026-06.jsonl` |
| W6 live-3 | kube-system `--dry-run false` → sandbox **blocked** | `reason: sandbox_blocked`；无生产 ns 写 |
| W6 live-4 | 全图 inspect + stream：`sandbox_run` delete 成功；`skill_verification.reason=sandbox_crash_loop_returned` | thread `5ad00ee0-6f4d-5cd6-a021-99469a86e4e1`；**restart langgraph** 后通过 |
| P0 verify | `verify-sentinel-x.sh` 全绿 | `list_pods count=8`；MCP 2 pods；langgraph active |

---

## 3. 未完成 / 推迟

| 任务 ID | 原因 | 挪至 |
|---------|------|------|
| W2-4 LLM 叙事 | DashScope timeout；template 回退可用 | 可选 |
| 一键部署 P0（新机） | 本次为既有服务器增量验收；全栈 install 已在 W22 环境 | 新机独立跑 `install-sentinel-x.sh` + verify 仍待复验 |

---

## 4. 阻塞与风险（验收期已缓解）

| 问题 | 影响 | 处理 |
|------|------|------|
| `/kube/config: permission denied` | 沙箱 kubectl 失败 | `executor.py` `docker run --user 0:0`；改代码后 **必须** `systemctl restart sentinel-langgraph` |
| busybox ImagePullBackOff | fixture Pod 无法 CrashLoop | `docker.m.daocloud.io/library/busybox:1.36` + `k3s ctr images import` |
| crash-demo `rollout status` 超时 | 误以为部署失败 | CrashLoop 永不 Ready；用 `kubectl get pods`，勿等 rollout |
| CLI 沙箱 OK / 图内 sandbox 失败 | 全图 stream 仍报 permission denied | LangGraph 进程加载旧 executor；restart + sync |
| 双 ReplicaSet 并存 | sleep ↔ exit 1 切换时两 Pod | 旧 RS `scale --replicas=0` |

---

## 5. 指标快照（验收完成时）

| 指标 | 数值 | 备注 |
|------|------|------|
| 单测 | sandbox + skills 新增 ~20 用例 | Graph smoke：`test_sandbox_run_node_mocked` |
| LangGraph thread_id | `5ad00ee0-6f4d-5cd6-a021-99469a86e4e1` | cluster=k3s-prod |
| `list_pods` | count=8 | 含 sentinel-sandbox sync |
| MCP K8s | 2 pods（kube-system） | verify 通过 |
| 沙箱 audit | `sandbox/audit/audit-2026-06.jsonl` | 含 `status: ok` 与 `status: blocked` |
| Skills | `fix-crashloop-restart.md` hit_count=11 | FTS + record_skill live |
| 代码修复入库 | `executor.py` `--user 0:0`；`sandbox/Dockerfile` 去 `USER 1001` | |

---

## 6. 下周计划（W7）

- [ ] 告警 / Webhook 触发 inspect
- [ ] Checkpoint Phase 2 spike（Postgres vs SQLite checkpointer）
- [ ] 可选：新机 one-shot `install-sentinel-x.sh` 独立复验

---

## 7. P0 Live 验收补充（2026-06-09）

**环境**：`root@sentinel-x`，`/opt/sentinel-x`；`CLUSTER_ID=k3s-prod`；全程 **未** 设置 `SENTINEL_EXECUTE_LIVE=1`。

| Step | 操作 | 期望 / 结果 |
|------|------|-------------|
| 0 | `SENTINEL_ROOT`、`K3S_KUBECONFIG`、`LANGGRAPH_RUN_LIVE=1` | 环境就绪 |
| 1 | `executor.py` 补丁 `docker run --user 0:0`（k3s.yaml mode 600） | kubeconfig 可读 |
| 2 | `docker run … get pods -n sentinel-sandbox` | 无 permission denied |
| 3 | busybox 镜像站 + `ctr import`；清理双 RS | CrashLoopBackOff（非 ImagePullBackOff） |
| 4-A | `sandbox_demo.py` + crash-demo | delete `exit_code=0`；`sandbox_crash_loop_returned`；`ok=false` 正常 |
| 4-B | patch `sleep 3600` → 再跑 `sandbox_demo.py` | `ok=true`；`sandbox_pass`；`ready_seconds=34` |
| 5 | kube-system `inspect` stream `--dry-run false` | `blocked=true`；`sandbox_blocked` |
| 6 | sync `sentinel-sandbox` + `inspect_langgraph_live_demo.py --dry-run false` + stream 补验 | `sandbox_pending=true`；图内 delete `exit_code=0` |
| 7 | `verify-sentinel-x.sh` | All checks passed |

**验收结论**：W1–W6 P0 live 闭环完成（查 / 判 / 试 / 记 均在生产服务器走通）。

**Runbook**：运维本机桌面 `Sentinel-X-P0-LIVE-验收手册.md`（§9.5 W6 收尾命令块）；repo 内 [`sandbox/README.md`](../../sandbox/README.md)、[`deploy/verify/verify-sentinel-x.sh`](../../deploy/verify/verify-sentinel-x.sh)。

---

## 8. 备注

- 总纲：[`docs/ROADMAP.md`](../ROADMAP.md)
- 架构评审：[`docs/ARCHITECTURE-REVIEW.md`](../ARCHITECTURE-REVIEW.md)（v2.1 同步 live 状态）
- W6 关键：`dry_run=false` 走沙箱；仅 `sentinel-sandbox` 允许 kubectl 写；生产 ns → policy block
- LangGraph 改 sandbox 代码后：`sudo systemctl restart sentinel-langgraph` + `/usr/local/bin/sentinel-sync-k8s.sh`
