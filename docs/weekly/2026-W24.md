# Sentinel-X 周总结 — 2026-W24

**周期**：2026-06-05 ~ 2026-06-11  
**对应路线图周次**：W7 — 告警入口 + 半自动闭环  
**填写人**：Agent

---

## 1. 本周目标（计划）

- [x] W7-1 cron 巡检 → 共享 `trigger_inspect`
- [x] W7-2 FastAPI 薄层（`/v1/inspect` + Alertmanager webhook）
- [x] W7-3 E2E demo + deploy 文档
- [ ] W7-4 服务器 live 验收（DEPLOY-ALERT-INSPECT §验收清单）

---

## 2. 本周完成

| 任务 ID | 内容 | 证据 / 链接 |
|---------|------|-------------|
| W7-1 | `src/trigger/`：`inspect_trigger.py`、`patrol.py`、`config.py` | `tests/test_inspect_trigger.py`、`tests/test_trigger_patrol.py`（6 用例通过） |
| W7-1 | Live patrol CLI + deploy 脚本 | `scripts/live/inspect_patrol_live.py`；`deploy/sync/sentinel-inspect-patrol.sh` |
| W7-2 | `apps/api` FastAPI | `POST /v1/inspect`；`POST /v1/webhooks/alertmanager`；`GET /health` |
| W7-2 | systemd + install 挂钩 | `deploy/systemd/sentinel-api.service`；`install-sentinel-x.sh --with-api` |
| W7-3 | E2E demo | `scripts/demo/alert_to_inspect_demo.py`（patrol / api 两路径） |
| W7-3 | 文档 | [`DEPLOY-ALERT-INSPECT.md`](../deploy/DEPLOY-ALERT-INSPECT.md)；ROADMAP / README 同步 |

---

## 3. 未完成 / 推迟

| 任务 ID | 原因 | 挪至 |
|---------|------|------|
| W7-4 live 验收 | 代码在本机完成；未在 `sentinel-x` 服务器跑 patrol cron | 部署后按 DEPLOY-ALERT-INSPECT Step 2–3 |
| Alertmanager 规则定制 | 超出 W7 范围 | W8+ |
| API 单元测试 | 计划仅要求 trigger 单测 | 可选补充 |

---

## 4. 阻塞与风险

| 问题 | 影响 | 处理 |
|------|------|------|
| patrol 依赖图中 Pod 状态 | 无 sync 则 `no_candidates` | cron sync 先于 patrol；或 `--sync-first` |
| 5min cron + 1h cooldown | 首次 trigger 后同 Pod 不再重复 | 符合设计；调试可删 state 文件 |
| API 默认无 Bearer | 仅 bind localhost | 文档警告；生产设 `SENTINEL_API_TOKEN` |

---

## 5. 指标快照

| 指标 | 数值 | 备注 |
|------|------|------|
| 单测（W7 trigger） | 6 passed | `test_inspect_trigger` + `test_trigger_patrol` |
| 新增模块 | `src/trigger/`、`apps/api/` | 共用 `stream_sentinel_run` |
| LangGraph thread_id | `5ad00ee0-6f4d-5cd6-a021-99469a86e4e1` | 与 W23 一致 |
| Live patrol | 未跑 | 待 `sentinel-inspect-patrol.sh` on server |

---

## 6. 下周计划（W8+）

- [ ] W7 live 验收：`sentinel-inspect-patrol.sh` + 可选 `--with-api`
- [ ] Checkpoint Phase 2 spike（Postgres vs SQLite checkpointer）
- [ ] 新机 one-shot `install-sentinel-x.sh` 独立复验

---

## 7. 备注

**架构摘要**：

```text
sentinel-inspect-patrol.sh ──► patrol.py ──► trigger_inspect ──► LangGraph
POST /v1/inspect ──────────────────────────► trigger_inspect
POST /v1/webhooks/alertmanager ────────────► trigger_inspect
```

**Live 验收命令预览**（见 [DEPLOY-ALERT-INSPECT.md](../deploy/DEPLOY-ALERT-INSPECT.md)）：

```bash
sudo bash deploy/install/install-deploy-scripts.sh
/usr/local/bin/sentinel-inspect-patrol.sh
tail -20 /var/log/sentinel-patrol.log
```

期望：`issues` 含 `CrashLoop`；默认 `dry_run=true` → `execution.status=simulated`。
