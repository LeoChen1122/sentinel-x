# Sentinel-X 周总结 — 2026-W22

**周期**：2026-05-26 ~ 2026-06-03  
**对应路线图周次**：W1 + W2 + W3 + W4 — Phase 1b 运维、Inspect E2E、Prometheus 进图、Streamlit UI（**均已完成 live 验收**）  
**填写人**：（待填）

---

## 1. 本周目标（计划）

### W1

- [x] W1 前置：K8s MCP 容器可读 Pod
- [x] W1 前置：k3s 控制面恢复（离线 airgap）
- [x] W1 前置：`mcp_k8s_sync_live.py` 首通 sync
- [x] W1-1 Live query 验收（`list_pods` / `events_for_pod`）
- [x] W1-2 langgraph systemd 常驻
- [x] W1-3 cron 增量 sync
- [x] W1-4 MCP kubeconfig / compose 固化入库
- [x] W1-5 `deploy/` 模板入库
- [x] W1-6 服务器与仓库 `mcp_k8s.py` 对齐

### W2

- [x] W2-1 选目标 Pod（coredns + sentinel-crash-test）
- [x] W2-2 inspect live（`--thread-only`，真实 sync 图）
- [x] W2-3 execution dry-run（Path B：`CrashLoop` → `restart_pod` simulated）
- [ ] W2-4 可选 LLM（DashScope timeout，已回退 template）
- [x] W2-5 文档 [DEPLOY-INSPECT-LIVE.md](../DEPLOY-INSPECT-LIVE.md)

---

## 2. 本周完成

| 任务 ID | 内容 | 证据 |
|---------|------|------|
| — | k3s 离线安装；API 恢复 | `/var/lib/rancher/k3s/agent/images/` |
| W1-6 | `mcp_k8s.py` docker exec 改为 `python -` + stdin | sync 首通 |
| — | Live sync 首通 | 7 pods, 65 events → 66 entities, 34 edges |
| W1-1 | `list_pods` count=7；`events_for_pod`(coredns) count=6 | thread `5ad00ee0-6f4d-5cd6-a021-99469a86e4e1` |
| W1-2 | `sentinel-langgraph.service` + DEPLOY-LANGGRAPH-SYSTEMD | systemd enable/start |
| W1-3 | `sentinel-sync-k8s.sh` + cron；增量 `skipped=7` | `/var/log/sentinel-sync.log` |
| W1-4 | kubeconfig `192.168.22.140:6443`；compose `extra_hosts` | 容器内 `k8s_get_pods` → 7 |
| W1-5 | deploy 脚本 + 四份 DEPLOY 文档 | `deploy/` |
| W2 Path A | coredns + `--thread-only`：`WarningEvents` → `review_events` | severity=warning, exit 0 |
| W2 Path B | sentinel-crash-test：`CrashLoop` → `restart_pod` simulated | severity=critical, exit 0 |
| W2-5 | [DEPLOY-INSPECT-LIVE.md](../DEPLOY-INSPECT-LIVE.md) | Path A/B 流程 + 验收表 |
| W3-0 | kube-prometheus-stack 离线安装；NodePort 30909 | `curl 127.0.0.1:30909/-/ready` |
| W3 live | `mcp_prom_sync_live.py` → `entities=7`；`top_pods_by_cpu` 含 `cpu_cores` | metrics-server 最高 CPU；thread 同上 |
| W3-5 | [DEPLOY-PROM-SYNC.md](../DEPLOY-PROM-SYNC.md) + [DEPLOY-PROMETHEUS-K3S.md](../DEPLOY-PROMETHEUS-K3S.md) | MCP `host.docker.internal:30909` |
| W4 live | Streamlit UI SSH 隧道 `:8501`；8 pods + cpu/memory 列 | [DEPLOY-UI-LIVE.md](../DEPLOY-UI-LIVE.md)；Connected to LangGraph |

---

## 3. 未完成 / 推迟

| 任务 ID | 原因 | 挪至 |
|---------|------|------|
| W2-4 LLM 叙事 | `llm_error=Request timed out`；template 回退可用 | W23 可选 |
| W1-3 观察 | cron 24h 稳定观察（可选） | W23 初若需补记 |
| sentinel-crash-test 清理 | 测试 Pod 可删 | 运维自行 `kubectl delete` |

---

## 4. 阻塞与风险（已缓解 / 残留）

| 问题 | 影响 | 处理 |
|------|------|------|
| 服务器无法拉 GitHub | scp 部署 | deploy 文档；`.gitattributes` LF |
| Windows scp → CRLF | `bash\r` / cron 不触发 | `sed -i 's/\r$//'` |
| docker-compose 1.29 ContainerConfig | recreate 失败 | `docker rm` + `up -d` |
| langgraph dev 内存 checkpoint | 重启丢图 | W1-3 cron 重建 |
| DashScope LLM timeout | narrative 回退 template | 加大 timeout 或关 LLM |

---

## 5. 指标快照（周末）

| 指标 | 数值 | 备注 |
|------|------|------|
| 单测（本机） | ~175 passed | langgraph-integration |
| Live sync | pods=7+ | kube-system, cluster=k3s-prod |
| LangGraph thread_id | `5ad00ee0-6f4d-5cd6-a021-99469a86e4e1` | |
| MCP 容器 | `mcp-servers_mcp-k8s_1` | server=`192.168.22.140:6443` |
| Inspect Path A | coredns | WarningEvents / review_events |
| Inspect Path B | sentinel-crash-test | CrashLoop / restart_pod simulated |
| cron | `*/5` root crontab | 建议只保留一条 |
| Prom sync live | pods=7, prom_cpu_series=8 | `sync ok: entities=7 skipped=0` |
| `top_pods_by_cpu` | metrics-server 最高 | cpu_cores + memory_bytes 非空 |
| MCP Prom | `mcp-servers_mcp-prometheus_1` | `PROMETHEUS_BASE_URL=host.docker.internal:30909` |
| Prometheus | kube-prom NodePort 30909 | monitoring namespace |
| Streamlit UI | 8 pods in kube-system | SSH `-L 8501`；metrics-server CPU 最高 |

---

## 6. 下周计划（W23 / ROADMAP W5）

### W4（已完成 live）

- [x] W4-1 Streamlit UI：Pods / Top CPU / Inspect
- [x] W4-2 DEPLOY-SERVER.md + DEPLOY-UI-LIVE.md
- [x] W4-3 README 结构对齐
- [x] W4-4 graph smoke 测试
- [x] W4 **live 验收**：浏览器 8 pods + cpu_cores/memory 列

### W23 计划

- [ ] W5 Skills 基础（`skills/` + 检索）
- [ ] W3 可选：Prom cron（`sentinel-sync-prom.sh`）
- [ ] W2-4 可选：修复 LLM timeout + `--llm` 验收

---

## 7. W4 补充（2026-06-04，live 验收 同日）

| 任务 ID | 内容 | 证据 |
|---------|------|------|
| W4-1 | Streamlit UI：Pods / Top CPU / Inspect | `apps/ui/app.py` |
| W4-2 | 部署总览 + UI 服务器步骤 | [DEPLOY-SERVER.md](../DEPLOY-SERVER.md)、[DEPLOY-UI-LIVE.md](../DEPLOY-UI-LIVE.md) |
| W4-3 | 根 README 结构 + Implementation status | `README.md` |
| W4-4 | graph 节点 smoke 测试 | `agents/langgraph-server/tests/test_graph_nodes.py`（4 passed） |
| W4 live | SSH 隧道 `127.0.0.1:8501`；Connected LangGraph | 8 pods；cpu_cores / memory 列；可选 systemd `sentinel-ui` |

---

## 8. 备注

- 总纲：[`docs/ROADMAP.md`](../ROADMAP.md)
- 部署：DEPLOY-SERVER、DEPLOY-UI-LIVE（W4 live）、DEPLOY-LANGGRAPH-SYSTEMD、DEPLOY-SYNC-CRON、DEPLOY-MCP-KUBECONFIG、DEPLOY-INSPECT-LIVE（W2）、DEPLOY-PROM-SYNC + DEPLOY-PROMETHEUS-K3S（W3）
- W2 关键：`LANGGRAPH_RUN_LIVE=1` + `--thread-only`（不用 `--live`）
- W4 UI：`streamlit run apps/ui/app.py` + `ssh -L 8501:127.0.0.1:8501`（见 [DEPLOY-UI-LIVE.md](../DEPLOY-UI-LIVE.md)）
